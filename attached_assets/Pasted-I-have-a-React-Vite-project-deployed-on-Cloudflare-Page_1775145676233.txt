I have a React + Vite project deployed on Cloudflare Pages. The app uses client-side rendering (CSR) with dynamic routes like `/tools/:slug` handled by `ToolPage.tsx`.

🚨 Current Problem:

* All pages return empty HTML (`<div id="root"></div>`) in view-source
* SEO content (H1, description, FAQ, etc.) is rendered only after JavaScript loads
* This causes Soft 404 and indexing issues in Google

━━━━━━━━━━━━━━━━━━━━━━━
✅ VERY IMPORTANT CONDITIONS
━━━━━━━━━━━━━━━━━━━━━━━

* DO NOT change any existing functionality
* DO NOT modify UI, layout, or styling
* DO NOT remove or rewrite any existing SEO content
* DO NOT change routing system
* DO NOT change tool logic or components
* DO NOT introduce SSR or Next.js
* KEEP everything exactly the same — only enhance rendering

━━━━━━━━━━━━━━━━━━━━━━━
🎯 GOAL
━━━━━━━━━━━━━━━━━━━━━━━
Upgrade the app so that ALL pages are prerendered into static HTML during build, while keeping the app fully client-side.

━━━━━━━━━━━━━━━━━━━━━━━
⚙️ REQUIRED SOLUTION
━━━━━━━━━━━━━━━━━━━━━━━
Use `react-snap` to prerender ALL pages.

━━━━━━━━━━━━━━━━━━━━━━━
🛠 TASKS
━━━━━━━━━━━━━━━━━━━━━━━

1. Install and configure `react-snap`

2. Update `package.json`:

   * Add `"postbuild": "react-snap"`
   * Ensure it runs after Vite build

3. Automatically generate ALL routes for prerender:

   A. Tool pages:

   * Read all tool slugs from `src/data/tools.ts`
   * Generate routes:
     `/tools/{slug}`

   B. Include homepage:
   `/`

   C. Include category pages:
   `/category/{categorySlug}`

   D. Include any static pages if present:
   `/about`, `/contact`, etc.

4. Configure react-snap:

   * Include ALL generated routes
   * Ensure it waits for content to fully render
   * Preserve React Helmet meta tags
   * Ensure JavaScript content is captured properly

5. Ensure prerender captures FULL existing content:

   * H1 title
   * Description paragraphs
   * Tool UI container
   * How-to steps
   * Benefits
   * H2 SEO sections
   * FAQ section
   * Popular searches
   * Related tools
   * JSON-LD schema

6. Ensure:

   * Output HTML contains real content (not empty root div)
   * No blank `<div id="root">` in final output
   * Routing works exactly the same after deploy
   * No JavaScript errors introduced

7. Ensure compatibility with Cloudflare Pages (static hosting)

━━━━━━━━━━━━━━━━━━━━━━━
🚫 DO NOT
━━━━━━━━━━━━━━━━━━━━━━━

* Do NOT refactor components
* Do NOT change ToolPage logic
* Do NOT change tools data structure
* Do NOT introduce SSR
* Do NOT remove React Helmet
* Do NOT break navigation or routing

━━━━━━━━━━━━━━━━━━━━━━━
✅ EXPECTED RESULT
━━━━━━━━━━━━━━━━━━━━━━━

After build:

* `/tools/images-to-pdf` should contain:
  `<h1>Images to PDF Converter</h1>`
  `<p>Description...</p>`
  `<h2>FAQ</h2>`

* Homepage `/` should contain real HTML content

* Category pages should also contain content

* All SEO content must remain EXACTLY the same

* Website behavior and UI must remain unchanged

━━━━━━━━━━━━━━━━━━━━━━━
📦 OUTPUT REQUIRED
━━━━━━━━━━━━━━━━━━━━━━━

Provide:

* Updated `package.json`
* react-snap configuration
* Script to auto-generate ALL routes from tools.ts
* Any minimal safe setup required

Focus ONLY on upgrading rendering for SEO while keeping everything else untouched.
